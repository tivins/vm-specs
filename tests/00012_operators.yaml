# tests/00012_operators.yaml

---
title: "Testing ternary, nullish coalescing, elvis operators"
expected_exit_code: 0
expected_stdout: ""
---

namespace test.operators;

class OperatorsTest {
	public void run() {
		bool a = false;
		string s1 = a ? "true" : "false";

		int x = 10;
		int y = x > 5 ? 100 : 200;

		OperatorsTest obj = null;
		string s2 = obj ?? "is null";

		int zero = 0;
		string s3 = zero ?: "not applicable";

		int v = 42;
		string s4 = v ?: "default";
	}
}
